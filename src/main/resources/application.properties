spring.application.name=gaminion-api

# Database connection
spring.datasource.url=jdbc:postgresql://localhost:5432/gaminion_db
spring.datasource.username=postgres
spring.datasource.password=joyceorog1A!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Summary card storage
gaminion.storage.path=uploads/summary-cards

# Server port
server.port=8080